# 更新日志 CHANGELOG

## Unreleased

### 新增
- **赛前弃赛（单场级）**：对阵卡片展开后可直接选择「右弃·左胜」/「左弃·右胜」。胜方正常计入个人胜场；该场整体不计入对手胜率 / 对手对手胜率 / 对手局胜率网络；弃赛者本人不记个人败场（1-1 选手第 3 轮赛前弃赛，个人胜率仍为 0.5，对手侧仍按 1-1 计入）。
- **帮助页免责声明**：新增独立 section，说明系统不一定适用于所有赛制、使用后果自负。

### 修改
- **战绩分组键改为仅看胜场**：瑞士轮第 2 轮起的配对分组只按「胜场相同」归为一组（不再要求所有排名指标完全相同）；组内排序仍按完整排名链（胜率 → 对手胜率 → 局胜率 → …）。
- **对手胜率 / 对手局胜率 / 对手对手胜率 统一改为聚合公式**：`Σ对手胜场 / Σ对手总场次`、`Σ对手胜局 / Σ对手总局`、对手对手同理摊平。弃赛选手自然因场次少而降权；赛前弃赛 match 整体从对手网络剔除。
- **弃赛规则拆分为赛前弃赛 vs 赛后弃赛**：赛后弃赛（控制面板「弃赛管理」）逻辑不变。
- **确认对话框 + 摘要区 UI 限高**：多小组同时开赛且弃赛选手多时，弹窗最高限制在 85vh；摘要区与各卡片内部列表独立滚动，确认按钮永远钉在底部可直接点击。
- **帮助页更新**：战绩分组键、赛前弃赛/赛后弃赛差异化规则、排名公式口径同步更新。

### 文档
- 新增 `README.md`（项目介绍、功能特性、启动指南、免责声明）。
- 新增本 `CHANGELOG.md`。
- 删除所有 UI / 默认命名 / 文档中的版本号。

### 修复
- 多小组场景下确认弹窗过长导致无法开启下一轮：引入多段式滚动布局。
- 赛前弃赛败方（弃赛者本人）个人 `losses` 不再累加，保持原战绩冻结。
- 撤回赛前弃赛结果时，败方不会多扣一个 `losses`（与 apply 侧对称实现）。

## 历史版本（此前无 CHANGELOG）
- 项目初始化：瑞士轮配对、单败淘汰、多小组、多局赛制、BO1-BO7、Excel/图片/JSON 导出、测试模式、撤回、Ctrl+Z、弃赛管理。
