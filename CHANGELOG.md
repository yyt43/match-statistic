# 更新日志 CHANGELOG

## Unreleased

### 新增

- **Excel 批量录入选手名单**：新增选手导入解析模块，支持从文本、Excel / CSV / TXT 文件中批量导入选手，自动按换行、逗号、分号拆分，过滤空白、重复和表头字段，并支持用户在导入前手动选择某个表头作为选手名称列，避免把分数、排名、备注等无关列误导入。

- **多表格 Excel 导入**：支持按工作簿中的 sheet 名称自动识别小组，单个 Excel 文件可同时导入多个小组名单；sheet 名会作为小组名，内容会自动拆分到对应分组中；每个 sheet 可单独指定最终使用的姓名列。

- **网站文本适配与项目元信息**：补充中文站点描述、关键词和 GitHub Pages 发布元信息，确保页面在浏览器标签和搜索抓取时呈现中文项目信息，并同步反映多表格 Excel 导入能力。

- **版权声明与联系方式**：网站底部（Home 页脚 + HelpPage 底部）新增 `© 2026 ShiyiPai. All rights reserved.` 版权声明；HelpPage「相关链接」新增开发者 QQ（2845850691）卡片；README「相关链接」补充开发者署名 ShiyiPai、QQ 联系方式；`package.json` 设 `name: match-statistic`、`author: ShiyiPai`。

### 调整

- **弃赛管理 UI 统一**：弃赛管理区块改为与赛制管理、选手管理一致的折叠式布局（`space-y-2` 标题 + `mt-3 space-y-2` 内容）；标题字号统一为 `text-xs text-slate-400`；「手动退赛(不必再点)」→「确认退赛」；「已赛前弃赛」→「已退赛」（该列表实际包含赛前弃赛与赛后弃赛的选手）；底部提示条拆为三段更清晰；HelpPage 同步更新弃赛入口描述。

- **加赛流程规则修正**：同分组选项分别抽签，不再混合配对；两人加赛允许再次交手，不再出现双方都轮空；三人加赛支持「三进一」和「三进二」模式，等候选手不自动获得胜场；四人首轮后可继续生成胜者组与败者组；最终名次按加赛赛程结果决定，不再仅按加赛胜场数排序；改判首场会清除受影响后续加赛，支持保存/刷新恢复；加赛不影响常规战绩与小分。

- **下移组内部配对修正**：奇数人数时优先配成可进行的比赛，再让剩余选手下移；无法全部配对时尽量保留可行对阵，不再整组全部下移；原有战绩分组、上下匹配优先级保持不变。

- **轮空统计修正**：每次轮空分别计入对手胜率及对手的对手胜率，不再合并为一次；加载/导入历史比赛时重新计算相关小分；原有轮空比分与计胜规则保持不变。

### 新增

- **加赛功能**：比赛结束后，若多名选手所有破分指标完全相同，排行榜显示「需加赛」徽章；控制面板「生成加赛」按钮一键检测平分选手并生成加赛对阵（遵循不重复对阵原则，奇数人轮空，加赛标记 `isPlayoff=true`）。**加赛结果只用于区分名次，不计入对手胜率/SOS/SOSOS/本人局胜率/对手局胜率等任何小分指标**，以独立 `playoffWins` 字段存储，排行榜显示「加赛N胜」徽章。`sortPlayersByRank` 在所有常规指标相同时以 `playoffWins` 作为最终破分键。

- **错误边界 ErrorBoundary**：全局包裹 `ErrorBoundary`，子组件渲染崩溃时显示错误信息 + 重试 + 导出数据按钮，防止白屏丢数据。

- **赛前弃赛（对阵级入口）**：对阵卡片展开后可直接选择「赛前弃·右弃权·左胜」/「赛前弃·左弃权·右胜」。胜方正常计入个人胜场；该场整体不计入对手胜率 / 对手对手胜率 / 对手局胜率网络；弃赛者本人不记个人败场（1-1 选手第 3 轮赛前弃赛，个人胜率仍为 0.5，对手侧仍按 1-1 计入）；弃赛者本人自动进入退赛状态（`dropped=true`），后续所有轮次不再安排对阵，与赛后弃赛等效。

- **帮助页免责声明**：新增独立 section，说明系统不一定适用于所有赛制、使用后果自负。

- **帮助页相关链接**：新增 GitHub 项目地址 + 开发者 B 站主页区块，带 a11y aria-label。

- **README 新增「轮空 / 赛前弃赛 / 赛后弃赛 — 行为与胜率影响总览」**：12 维度对比表 + 4 个典型例子。

- **CI 工作流**：新增 `.github/workflows/ci.yml`，PR/push 时自动跑 `npm run check` + `lint` + `test`。

- **Prettier 配置**：新增 `.prettierrc` + `.prettierignore` + `npm run format` 脚本，统一代码格式。

- **移动端响应式**：`MatchImageView` 对阵表导出从固定 4 列改为移动端 2 列 + 桌面端 4 列（`grid-cols-2 md:grid-cols-4`）。

- **可访问性 a11y**：帮助页关闭按钮、相关链接等关键交互元素加 `aria-label` / `role` 属性。

### 修改

- **排名破分链按赛制定制（瑞士轮）**：

  - **BO1**：活跃状态 → 胜场数 → 对手胜率(SOS) → 对手的对手胜率(SOSOS) → points

  - **BO3 / BO5 / BO7**：活跃状态 → 胜场数 → 对手胜率(SOS) → 本人局胜率 → 对手局胜率 → points

  - （删除旧版首项「个人胜率 winRate」作为破分键；BO3+ 不再使用 SOSOS 作为破分指标，与官方文档口径一致）

- **战绩分组键改为仅看胜场**：瑞士轮第 2 轮起的配对分组只按「胜场相同」归为一组（不再要求所有排名指标完全相同）；组内排序仍按完整排名链。

- **对手胜率 / 对手局胜率 / 对手对手胜率 统一改为聚合公式**：`Σ对手胜场 / Σ对手总场次`、`Σ对手胜局 / Σ对手总局`、对手对手同理摊平。弃赛选手自然因场次少而降权；赛前弃赛 match 整体从对手网络剔除。

- **弃赛规则拆分为赛前弃赛 vs 赛后弃赛**：

  - 赛后弃赛（控制面板「赛前 / 赛后弃赛管理」→「赛后弃赛」按钮）：不产生新的胜负归属给他人，仅手动标记退赛。

  - 赛前弃赛（对阵卡片「赛前弃」按钮）：有明确对阵，对方记胜场、弃赛方不记败场、不入对手胜率网络、弃赛方自动 dropped=true 退赛。

- **ControlPanel 文案统一**：去掉歧义句"仅触发时间点不同"，明确说明两者真实区别（触发方式+胜负归属）；区域标题改为「赛前 / 赛后弃赛管理」；「已赛后弃赛」小标题改为「已赛前弃赛」；各按钮 Tooltip、角标、警戒按钮、底部提示条同步对齐（共 13 处）。

- **MatchList 赛前弃赛按钮 UI**：3 行分层显示（首行小徽章「赛前弃」/ 次行「X弃权·Y胜」/ 末行「X不记败场」），明确区分赛后弃赛。

- **确认对话框 + 摘要区 UI 限高**：多小组同时开赛且弃赛选手多时，弹窗最高限制在 85vh；摘要区与各卡片内部列表独立滚动，确认按钮永远钉在底部可直接点击。

- **对阵表图片导出由 2 列改为 4 列**：`MatchImageView` 导出的对阵表图片布局 `grid-cols-2` → `grid-cols-4`，信息密度提升。

- **帮助页更新**：战绩分组键、赛前弃赛/赛后弃赛差异化规则、排名公式口径、FAQ「如何处理选手弃赛」「偶数人也会出现轮空」同步更新。

### 文档

- 新增 `README.md`（项目介绍、功能特性、启动指南、免责声明 + 轮空赛前赛后弃赛胜率影响对比表）。

- 新增本 `CHANGELOG.md`。

- 删除所有 UI / 默认命名 / 文档中的版本号。

- `types/index.ts` 的 `Match.preDrop` 字段注释补全：败方不记 losses、自动 dropped=true、与赛后弃赛的本质区别。

### 修复

- 多小组场景下确认弹窗过长导致无法开启下一轮：引入多段式滚动布局。

- 赛前弃赛败方（弃赛者本人）个人 `losses` 不再累加，保持原战绩冻结。

- 撤回赛前弃赛结果时，败方不会多扣一个 `losses`（与 apply 侧对称实现）；同时自动恢复 `dropped=false`（与 preDrop=true 时 set `dropped=true` 对称）。

- 赛前弃赛败方自动标记 `dropped=true`，后续配对入口 activePool 统一过滤，不再安排后续任何对阵。

### 目录整理

- 删除根目录旧压缩包 `swiss-tournament.tar.gz`、脚本 `test-reset.mjs` 以及 IDE 遗留的临时探测文件（`_______tmp.ts`、`________probe___.txt`）。

- 源码目录整体恢复到与 Git HEAD 一致，无未跟踪临时文件。

## 历史版本（此前无 CHANGELOG）

- 项目初始化：瑞士轮配对、单败淘汰、多小组、多局赛制、BO1-BO7、Excel/图片/JSON 导出、测试模式、撤回、Ctrl+Z、弃赛管理。

